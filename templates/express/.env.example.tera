# HTTP port the API binds to.
PORT={{ api_port }}

# Postgres / MySQL / SQLite – wire as needed.
DATABASE_URL=postgres://postgres:postgres@db:5432/{{ project_name_snake }}

# Used to sign JWTs. Generate with: openssl rand -hex 32
JWT_SECRET=replace-me-with-a-real-secret
