# Public site URL (used by NextAuth, redirects, ...)
NEXTAUTH_URL=http://localhost:{{ app_port }}

# Random 32+ character string. Generate with: openssl rand -hex 32
NEXTAUTH_SECRET=replace-me-with-a-real-secret

# Optional database connection
# DATABASE_URL=postgres://postgres:postgres@db:5432/{{ project_name_snake }}
