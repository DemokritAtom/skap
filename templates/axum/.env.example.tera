# HTTP port the service binds to.
PORT={{ api_port }}

# Optional database connection. Uncomment when you wire sqlx.
# DATABASE_URL=postgres://postgres:postgres@db:5432/{{ project_name_snake }}

# tokio / tracing log level (info, debug, trace, ...)
RUST_LOG=info,{{ project_name_snake }}=debug
